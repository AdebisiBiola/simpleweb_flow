name: hello flow
on:
  push:
    branches:
     - master
jobs:
  greetings:
    runs-on: ubuntu-latest
    steps:
     - name: "say hello world"
       run: echo "hello world...."

     - name: "test project"
       run: echo "how are you ???"